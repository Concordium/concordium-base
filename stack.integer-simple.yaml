# This file is intended to be used in an alpine-linux environment with an
# appropriate GHC to generate static binaries.

resolver: lts-16.20

packages:
- .

extra-lib-dirs:
    - lib

flags:
  hashable:
    integer-gmp: false
  scientific:
    integer-simple: true
  integer-logarithms:
    integer-gmp: false
